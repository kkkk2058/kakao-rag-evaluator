# 리트리버 설계 노트 (BM25 버전)

작성일: 2026-08-10 · 대상: 결과기 검색(retrieval)·청킹 모듈 (§8은 실전 검증 겸 생성 단계 버그 기록) · 관련 코드: [`결과기_팀구현.ipynb`](결과기_팀구현.ipynb) 1번 셀, [`하네스/`](하네스/)

---

## 1. 제약 조건 → 설계로 이어진 결정들

| 제약 | 결정 |
|---|---|
| 코퍼스가 72개 조항뿐 (약관 4종) | 벡터DB·ANN 인덱스 불필요. BM25만으로 충분한 규모 |
| 새 Colab T4 세션에서 위→아래로 한 번에 실행돼야 함 (실패 시 0점) | 임베딩 모델 생략, 리랭커는 `try/except`로 감싸 다운로드 실패 시 BM25 단독 폴백 |
| 질문이 숫자·조번호·기간 등 정확 일치가 정답을 가름 (예: "2시간", "8세 이하") | 의미검색(임베딩)보다 어휘검색(BM25)이 이 도메인에 유리 |
| 한국어는 조사가 붙음 ("제10조는" vs "제10조") | 형태소 분석기 대신 **문자 2-gram(bigram)** 토크나이즈로 조사 변형에 강건하게 |
| 반환 계약이 "실제 사용한 근거만 1~4개" 요구 | top-4 고정이 아니라 **점수 임계값 기반 동적 선택** |
| 원문 전체를 프롬프트에 넣는 방식 금지 | 조(條) 단위 청킹 — 채점 단위(`[문서명, 조번호]`)와 검색 단위를 일치시킴 |

---

## 2. 최종 구조 (RRF 융합 BM25 + 리랭커 2단계)

```
질문
 → ① BM25(문자 bigram)를 "조 전체"와 "항 단위 최고점" 두 가지로 각각 스코어링
     → 두 순위를 RRF(Reciprocal Rank Fusion)로 결합 → 상위 30개 후보
 → ② 리랭커(BAAI/bge-reranker-v2-m3)가 30개를 정밀 재정렬
 → ③ sigmoid 정규화 후 1등은 무조건, 2~4등은 확률 0.3↑일 때만 포함
 → 최종 1~4개 조항 반환
```

리랭커 로딩 실패 시 ②③을 건너뛰고 ①의 RRF 결합 점수를 최댓값으로 정규화해 동일한 임계값 로직을 적용한다(자동 폴백, 별도 조치 불필요).

### 2-1. 왜 "조 전체" 하나만으로는 부족한가 — RRF 도입 배경 (2026-08-10)

조 전체를 하나로 스코어링하면 긴 조항이 짧은 조항을 압도하는 문제(§5-2)가 구조적으로 있다. 리랭커가 이를 바로잡아 주지만, **리랭커가 죽으면(다운로드 실패 등) 이 편향이 그대로 노출**된다.

대안으로 "항(①②③, 1.2.3.) 단위로 쪼개 그 조의 최고점을 대표 점수로 쓰는" 방식을 테스트했으나, 이번엔 반대로 **문맥과 무관하게 짧고 밀도 높은 항이 과대평가**되는 새로운 실패가 생겼다(예: "탈퇴" 질문에서 "계정 생성 거절" 조항의 한 항이 계정 관련 단어만으로 오탐, P09가 1위→2위로 퇴보).

| 방식 | MRR (공개 10 + 패러프레이즈 4문항, 리랭커 없이) |
|---|---|
| 조 전체만 | 0.952 |
| 항 최고점만 | 0.893 (더 나쁨) |
| **RRF 결합** | **0.964** (최고) |

두 방식의 실패 유형이 정반대(하나는 긴 글 편향, 하나는 짧은 글 편향)이기 때문에 순위를 섞으면 서로 보완된다. 그래서 항 단위 점수를 단독으로 쓰지 않고 조 전체 점수와 **결합**만 한다.

**실제 노트북 파일로 재검증**: 리랭커 없이(폴백 경로) 공개 10문항 MRR = **1.000** (스모크 테스트 완료, 2026-08-10).

### 2-2. 부모-자식 청킹(parent-child chunking)과의 관계

표준 부모-자식 청킹은 "자식(작은 조각)으로 검색하고, 뽑히면 부모(큰 원문)를 LLM에 준다"는 구조다. 우리 설계는 이 아이디어의 절반만 그대로 쓰고 나머지 절반은 다르게 바꿨다.

**같은 부분 — "검색 단위 ≠ LLM에 주는 단위" 분리**
검색은 항(자식) 단위로도 하고, 뽑힌 뒤 LLM에는 조 전체(부모) 원문을 통째로 준다(§5-4에서 1000자 truncation을 없앤 것이 바로 이 부분). 표준 부모-자식과 동일한 발상이다.

**다른 부분 — 자식만으로 순위를 매기지 않는다**

| | 표준 부모-자식 | 우리 설계 |
|---|---|---|
| 순위를 매기는 기준 | 자식 점수만 | **자식 + 부모 점수를 RRF로 결합** |
| LLM에 주는 것 | 부모 전체 | 부모 전체 (동일) |
| 전제하는 검색 방식 | 보통 임베딩(의미 기반) | BM25(어휘 기반) |

표준 부모-자식이 "자식 단독 검색"으로 충분한 이유는, 보통 임베딩(의미 기반) 검색을 쓰기 때문이다 — 문장이 짧아도 의미를 이해하므로 핵심 단어가 없어도 관련성을 잡아낸다.

우리는 어휘 기반 BM25를 쓴다. 짧은 항일수록 우연한 단어 밀도에 낚이는 위험이 있다는 걸 §2-1 실험으로 실제로 확인했다(항 단독 MRR 0.893, 조 전체 단독보다 더 나쁨 — "탈퇴" 질문에서 "계정 생성 거절" 조항의 한 항이 계정 관련 단어만으로 오탐한 사례). 그래서 자식 단독 대신 부모 점수와 결합해 서로의 실패를 보완했다.

**한 줄 요약**: 부모-자식 청킹의 "부모를 통째로 준다"는 그대로 채택하고, "자식만으로 검색한다"는 부분은 우리 검색 방식(BM25)에 안 맞아 부모 점수와 섞는 쪽으로 바꿨다.

---

## 3. 청킹 — 조(條) 단위, 균일 길이 아님

- 반환/인용 단위 = 약관의 자연스러운 단위인 "제N조" 하나 (채점 형식이 조 단위이고, 질문이 조 전체 맥락(예: "5가지 항목 나열")을 요구하는 경우가 있어서)
- **검색 스코어링만** 항(①②③, 1.2.3.) 단위 서브청크로 추가 분할해 조 전체 점수와 RRF로 결합 (§2-1, §2-2 — 부모-자식 청킹과 유사하되 자식 단독이 아니라 부모와 결합)
- 길이는 그대로 둠 (짧으면 143자, 길면 3482자)
- ~~LLM 프롬프트에 넣을 때 1000자로 자름~~ → **제거함 (2026-08-10)**. 아래 5-4 참조
- 파싱 방법:
  - HTML 3종(계정약관/위치정보약관/통합서비스약관): "목차 → 본문"이 페이지에 두 번 나오는 구조를 이용, 두 번째 등장 지점부터 본문으로 파싱
  - PDF 1종(2022 통합약관): 문서 내 타 법령 인용("제16조 제2항" 등)으로 인한 오탐 방지를 위해 "지금 기대하는 조 번호와 정확히 일치할 때만" 경계로 인정

---

## 4. 검증 결과

| 검증 | 결과 |
|---|---|
| 공개 10문항, BM25 단독 (로컬, GPU 없이) | MRR = 1.000 (10/10 1위 적중) |
| 공개 10문항, 전체 파이프라인 (Colab 실측, 팀 8 결과) | MRR = 1.000 (10/10 1위 적중), 형식검사 통과 (fail=0, doc_name_violations=0) |
| 패러프레이즈 3문항 추가 (조사·어휘 변형) | 1단계(단순겹침) 0.611 → 2단계(BM25 단어) 0.833 → 3단계(BM25 bigram) **1.000** |

재현 스크립트: [`하네스/harness_retrieval.py`](하네스/harness_retrieval.py), [`하네스/retriever_progression.py`](하네스/retriever_progression.py)

---

## 5. 발견된 약점 (미해결)

### 5-1. 보일러플레이트 조항 중복 — 문서 특정 안 되는 질문에 취약 (BM25 단독 실패, 리랭커로는 원리적 한계)

4개 문서가 같은 회사·비슷한 시기에 작성돼 일부 조항이 거의 동일한 문장이다.

| 제목 | 겹치는 문서 수 | 최대 텍스트 유사도 |
|---|---|---|
| 약관 외 준칙 | 4개 문서 | 0.80 |
| 분쟁의 해결 | 3개 문서 | — |
| 개인정보의 보호 | 3개 문서 | — |
| 이용계약 해지 | 3개 문서 | — |
| 목적 | 3개 문서 | — |

질문이 특정 약관을 가리키는 고유 단서 없이 일반적으로 물으면("본 약관에 규정되지 않은 사항은 무엇에 따르나요?") BM25 단독에서는 상위 3개 문서의 점수가 오차 범위 안에서 붙었다.

**Colab 리랭커 재검증 결과 (2026-08-10)**: 리랭커를 붙여도 4개 문서가 여전히 상위권에 고르게 남는다 — 그러나 이는 버그가 아니다:

```
1위 카카오 통합 약관 제3조 (약관 외 준칙)
2위 카카오계정 약관 제3조 (약관 외 준칙)
3위 카카오 통합서비스약관 제3조 (약관 외 준칙)
4위 카카오 위치정보 이용약관 제3조 (약관 외 준칙)
```

4개 전부 **주제(카테고리)는 정확히 "약관 외 준칙"으로 맞혔다** — 리랭커가 할 수 있는 최선이다. 어느 회사 약관인지는 질문 자체에 그 정보가 없으므로 원리적으로 판단 불가능한 영역이며, 실제 공개 10문항은 전부 문서를 특정할 수 있는 고유 단서를 포함하고 있어 이런 완전 무단서 질문이 출제될 가능성은 낮다. 현재처럼 애매하면 후보 4개를 넓게 반환하는 편이 정답 누락보다 안전하다.

**결론: 실제 위험도를 "심각"에서 "낮음(참고용)"으로 하향** — 대응 로직(문서명 가중치 부스팅)은 우선순위를 낮춘다.

**추가 업데이트 (2026-08-10, §5-6과 연결)**: 질문이 문서명을 실제로 언급하는 경우엔 §5-6에서 발견한 별개의 버그(인덱스에 문서명 자체가 없던 문제) 때문에 그 신호가 애초에 무시되고 있었다. 이제 그 버그를 고쳐서, "질문에 문서명이 있으면" 훨씬 잘 구분된다(§5-6 참조). 5-1은 "문서명이 아예 없는 질문"에 대해서만 여전히 원리적 한계로 남는다.

### 5-2. 긴 조항이 짧은 조항을 압도함 — **리랭커로 해결 확인 (2026-08-10)**

BM25 단독일 때: "이용자의 개인정보는 어떤 목적으로 이용되나요?" 질문에서 정답("개인정보의 보호", ~150자)이 아니라 관련 없는 긴 조항("이용 방법 및 주의점", 3000자+)이 1~2위로 나왔다:

```
1위 카카오 통합 약관 제8조 (서비스 이용 방법 및 주의점)       ← 오답
2위 카카오 통합서비스약관 제12조 (통합서비스 이용 방법 및 주의점) ← 오답
3위 카카오계정 약관 제13조 (개인정보의 보호)                   ← 정답인데 3위
```

원인: 긴 문서일수록 흔한 단어(예: "정보")가 우연히 많이 포함될 확률이 높음. BM25의 길이 정규화로도 20배 가까운 길이 차이는 완전히 상쇄되지 않음.

**Colab에서 리랭커를 붙여 같은 질문 재실행 결과 — 완전히 뒤집힘**:

```
1위 카카오 통합 약관 제15조 (개인정보의 보호)       ← 정답 카테고리
2위 카카오계정 약관 제13조 (개인정보의 보호)        ← 정답 카테고리
3위 카카오 통합서비스약관 제14조 (개인정보의 보호)  ← 정답 카테고리
4위 카카오 통합 약관 제8조 (이용 방법 및 주의점)    ← 오답이 4위로 밀림
```

리랭커가 글자 겹침이 아니라 의미로 판단하면서 길이 편향이 사라졌다. **이 항목은 해결됨으로 종료.**

### 5-3. BM25 1차 풀링(pool=30) 후보 누락 위험 — **낮음으로 확인, 추가로 RRF 융합까지 도입 (2026-08-10)**

5-1·5-2에서 리랭커가 정답을 되찾아준 사례들의 **BM25 원시 순위**(리랭킹 전)를 확인한 결과, 전부 5위 이내였다(30위 밖으로 밀린 사례 없음). 72개 코퍼스에서 `bm25_pool=30`은 충분히 넉넉하다.

여기서 한 걸음 더 나아가, "조 전체 BM25 하나만으로 후보를 뽑는" 방식 자체가 리랭커 폴백 시 취약하다는 걸 확인하고 **§2-1의 RRF 융합 스코어링**으로 교체했다(조 전체 + 항 최고점 결합). 자세한 실험 근거는 §2-1 참조.

### 5-4. 프롬프트 truncation(1000자)으로 인한 정보 손실 — **발견 및 해결 (2026-08-10)**

72개 조항 중 14개(19%)가 1000자를 넘고, 최악의 경우(통합서비스약관 제12조, 3482자) **2482자가 통째로 잘렸다.** 실제로 잘려나간 부분을 까본 결과:

| 조항 | 잘리는 양 | 잘려나간 부분의 내용 |
|---|---|---|
| 통합서비스약관 제12조 | 2482자 | 금지행위 목록 12~23번 항목 전체 |
| 통합 약관 제17조 (인증서비스) | 1552자 | 인증서 폐지 사유 목록 전체 |
| 통합서비스약관 제6조 | 1362자 | ⑥⑦⑧항 전체 (AI 기반 서비스 고지 포함) |
| **통합 약관 제16조 (위치기반서비스)** | 543자 | **"8세 이하 아동 등 보호의무자 동의" 항목 전체** (공개 문항 P10과 동일 주제) |

검색은 정확한 조항을 찾아내도, 생성 단계에서 LLM이 잘려나간 부분의 사실을 아예 볼 수 없어 오답으로 이어질 수 있는 실제 위험 확인.

**조치**: `_build_user_prompt`에서 1000자 truncation 제거, 조항 원문 전체를 프롬프트에 포함하도록 수정 완료([`결과기_팀구현.ipynb`](결과기_팀구현.ipynb) 1번 셀). 가장 긴 조항도 한국어 기준 2000토큰 안팎이라 Qwen2.5 컨텍스트 한도(32k)에 여유 있고, 대회 규정상 HTTP 속도 자체엔 점수가 없어 정확도를 우선하는 판단. (리랭커 채점용 텍스트의 1000자 제한은 정보 손실과 무관해 그대로 둠 — 순위 계산에만 쓰이고 답변 생성에는 안 들어감.)

**후속 확인 필요**: 프롬프트가 길어지면서 지연시간(기존 p50=23s)이 얼마나 늘어나는지 Colab 재실측 필요.

### 5-5. 미조정 하이퍼파라미터

- `rel_threshold=0.3`: 리랭커 sigmoid 확률 컷오프, 감으로 설정. 아직 하네스 스윕 안 함

### 5-6. 인덱스 텍스트에 문서명이 빠져있던 버그 — **발견 및 해결 (2026-08-10)**

`_CORPUS_TEXT`를 `f"{title} {text}"`로만 만들고 **문서명(`doc`)을 아예 포함하지 않았다.** 그 결과 질문이 문서를 명시적으로 지목해도("카카오계정 약관에 규정 안 된 사항은 어떻게 되나요?") 그 신호가 인덱스 어디에도 없어 완전히 무시됐다.

```
버그 상태: "카카오계정 약관에 규정 안 된 사항은 어떻게 되나요?"
  1위: 카카오 통합서비스약관 제3조 (약관 외 준칙)   ← 오답, 6위로 밀린 정답
```

원인 규명: bigram 겹침을 직접 까보니, 오답으로 뽑힌 통합서비스약관 제3조 **본문 안에** "카카오 운영정책"이라는 무관한 문구가 우연히 들어있어 질문의 "카카오계정"과 "카카"·"카오" bigram이 겹쳤다. 반면 정답인 계정약관 제3조는 조항 본문 자체에 "카카오"라는 글자가 안 나와서(문서명에만 있고 본문엔 없음) 그 신호를 못 받았다 — 애초에 문서명이 인덱스에 없었으니까.

**조치**: `_CORPUS_TEXT`와 `_SUBCHUNKS` 둘 다 `f"{doc} {title} {text}"`로 수정. 재검증 결과 즉시 1위로 정정:

```
수정 후: 1위 카카오계정 약관 제3조 (약관 외 준칙) ← 정답
```

공개 10문항 회귀 없음(MRR 1.0 유지). §5-1이 "위험 낮음"으로 판단했던 근거(질문이 문서를 특정하면 괜찮을 것) 자체가 이 버그 때문에 실제로는 작동 안 하고 있었다는 뜻이라, 이번 수정으로 5-1의 실효 위험도 같이 낮아졌다.

### 5-7. 서브청크 분할 정규식 버그 — **발견 및 해결 (2026-08-10)**

§2-1에서 도입한 항 단위 분할(`_split_subchunks`)이 순수 lookahead 패턴(`(?=\d+\.[^\d])`)을 썼는데, `re.split`이 제로폭 매치 뒤 커서를 1글자만 전진시키는 특성 때문에 **두 자리 이상 번호("10." 이상)가 "1"과 "0.내용"으로 재분할**되는 버그가 있었다.

```
버그: 계정약관 제12조 서브청크 29개 중 10개가 "1" 한 글자짜리 파편
      (10.~17.번 항목이 전부 "1" + "0.다른 회원의..." 식으로 깨짐)
```

전체 코퍼스 스캔 결과 5자 미만 파편이 21개 있었다(항목이 10개 넘는 조항 — 계정약관 제12조, 통합서비스약관 제12조 등에 집중).

**조치**: 패턴에 `(?<!\d)` (음의 후방탐색, "숫자 앞에 또 숫자가 있으면 매칭 안 함")를 추가해 다자리 번호 중간에서 재매칭되지 않게 수정. 수정 후 파편 0개, 서브청크 364→343개로 정상화. 회귀 없음(MRR 1.0 유지).

### 5-8. 짧은 조항이 오히려 불리한 반대 방향 편향 — **테스트 결과 낮은 위험**

5-2(긴 조항 편향)의 반대 가설 — 아주 짧은 조항(85~123자)이 매칭할 bigram 자체가 적어서 오히려 검색에 불리하지 않은지 테스트. "위치정보 서비스 이용료는 얼마인가요?" (정답: 위치정보약관 제5조, 88자) 등으로 확인한 결과 1위로 정상 검색됨 — 짧다고 불리해지는 패턴은 발견되지 않음. 낮은 우선순위로 종료.

---

## 6. 다음 확인 사항 (우선순위 순)

1. ~~5-4+RRF+5-6+5-7 반영 후 Colab 재실행~~ — **완료 (§8 참조)**. 실제 제출 3회 전부 검색 MRR 1.000
2. `rel_threshold` 값을 하네스로 스윕해 근거 있는 값으로 교체 (아직 미착수)
3. (낮은 우선순위) 5-1이 실제로 문제가 되는 사례가 비공개 30문항 채점 후 발견되면(문서명조차 없는 완전 무단서 질문), 추가 보정 로직 검토 — 단 5-6 수정으로 실효 위험은 이미 크게 낮아짐
4. **[신규, 최우선]** 생성 프롬프트 수정 — §8-2 참조. 검색은 검증 끝났고 지금 병목은 전부 생성 단계

## 7. 검증 이력 (실제 노트북 파일 스모크 테스트)

2026-08-10: `결과기_팀구현.ipynb` 1번 셀을 torch/transformers/sentence-transformers를 스텁으로 대체해 실행 — 문법·변수 오류 없음 확인(직전에 변수명 중복 버그 `_CORPUS_TEXT_CORPUS_TEXT`를 실제로 한 번 잡아냄). GitHub raw URL 다운로드 정상 작동, 리랭커 없는 폴백 경로에서 공개 10문항 MRR = 1.000 재확인.

2026-08-10 (2차): §5-6(문서명 인덱스 누락), §5-7(서브청크 파편) 버그 수정 후 동일한 방식으로 재검증 — 두 수정 모두 반영된 상태에서 공개 10문항 MRR = 1.000 유지, 문서명 명시 질문("카카오계정 약관에...")도 정답 1위로 정정 확인.

---

## 8. 실제 Colab 제출 결과 평가 및 함정 스트레스 테스트 (2026-08-10)

### 8-1. 실제 제출 파일 평가 (총 3회)

팀 8이 Colab에서 실제로 `결과기_팀구현.ipynb`를 돌려 만든 `answers_public_8.json`을 3차례 받아 공개 10문항 gold와 대조 평가했다.

| 회차 | 검색 MRR | p50 / p95 지연 | 형식 검사 |
|---|---|---|---|
| 1회차 (RRF 도입 전) | 1.000 | 23.7s / 31.5s | 통과 |
| 2회차 (RRF+문서명+truncation 제거 반영) | 1.000 | 17.6s / 29.6s | 통과 |

**검색은 실전에서도 흔들림 없이 1.000을 유지했다.** truncation 제거로 프롬프트가 길어졌음에도 지연시간은 오히려 개선됐다(측정 노이즈일 수 있으나 최소한 악화되진 않음).

생성(답변 텍스트) 쪽에서는 매 회차 다른 버그가 확인됐다 — 상세는 §8-3.

### 8-2. 함정/충돌 스트레스 테스트 (T01~T06, 직접 제작)

운영진 공개 10문항이 다 쉬운 편이라, 실제 원문에서 예외조항·문서 간 충돌 지점을 직접 찾아 6문항을 만들고 정답을 원문 대조로 검증했다. 파일: [`하네스/trap_questions.json`](하네스/trap_questions.json), 실행 스크립트: [`하네스/trap_stress_test_harness.py`](하네스/trap_stress_test_harness.py).

| ID | 함정 유형 | 정답 문서 |
|---|---|---|
| T01 | "예외 없이 항상" 전제 뒤집기 (손해배상) | 계정약관 제16조 |
| T02 | "예외 없이 즉시" 전제 뒤집기 (위치정보 파기) | 위치정보약관 제9조 |
| T03 | "반드시 검토 의무" 부정 확인 | 통합서비스약관 제9조 |
| T04 | "전부 자유롭게 수정" 부정 확인 | 계정약관 제9조 |
| T05 | **문서 간 숫자 충돌** ("6개월" vs "6개월 이상") | 위치정보약관 제8조 |
| T06 | "무조건 승낙" 전제 뒤집기 (가입 승낙) | 계정약관 제6조 |

**검색 결과 (로컬, 리랭커 없이 RRF 폴백 경로)**: MRR = **1.000** (T01만 2위 — 5-1과 동일한 보일러플레이트 중복 패턴, 새 버그 아님).

**Colab 전체 파이프라인 결과 (2회 실행)**: 검색은 6문항 모두 1위 근처(T01만 2위, 5-1과 동일 원인)로 안정적이었으나, **생성 단계에서 재현 가능한 심각한 버그를 확정했다.**

### 8-2b. 검색 일반화 대규모 검증 (2026-08-10, 함정 4문항 추가 + 72개 조항 전수)

함정 세트에 원문 "다만" 예외절 후보(33개 중 미사용분)에서 4문항(T07~T10)을 더 추가했다. T10은 특히 "카카오 통합서비스약관에 따르면"처럼 문서명을 명시했을 때, 손해배상 조항이 3개 문서(계정16·통합서비스15·통합18)에 근접 중복돼 있음에도 정확히 그 문서로 가는지 테스트하는 문항이다.

| 검증 | 결과 |
|---|---|
| 함정 10문항 (T01~T10), RRF, 리랭커 없이 | MRR = **0.950** (T09만 2위) |
| T10 (3중 손해배상 보일러플레이트 + 문서명 명시) | **1위 정확** — 문서명 인덱스 수정(5-6)이 3중 충돌에서도 유효함을 실증 |

추가로 **72개 조항 전체를 대상으로 커버리지 전수 테스트**를 했다 — 각 조의 본문에서 가장 정보량이 많은 문장을 자동 추출해 질문을 만들고(문서명 힌트는 주지 않는 최악의 조건), 정확히 그 조가 1위로 나오는지 확인:

```
72개 조항 전수 커버리지: 62/72 (86.1%) 직접 1위 적중
```

나머지 10건을 전부 개별 확인한 결과 **100%가 이미 알려진 5-1 보일러플레이트 충돌 패턴이었다** — 제목이 미세하게 달라("개인정보의 보호" vs 제목 표기가 다른 동일 주제 등) 처음엔 "다른 주제"로 잘못 집계됐을 뿐, 새로운 실패 유형은 0건이었다. 이 테스트는 문서명 힌트를 전혀 안 준 최악의 조건이라는 점을 감안하면, 실제 질문(대부분 문서를 특정할 단서를 포함)에서는 이보다 안전할 것으로 판단.

**종합**: 공개 10문항 + 패러프레이즈 4문항 + 자체 함정 10문항 + 72개 조항 전수 = 90개 이상의 질의를 검증했고, 실패는 예외 없이 "문서명 단서가 전혀 없는 보일러플레이트 질문" 하나의 패턴으로 수렴했다. **검색 일반화 검증은 이것으로 종료.**

### 8-3. 생성 단계에서 확정된 버그 (검색 범위 밖, 다음 작업 대상)

**T05 — 문서 충돌로 인한 사실 오염 (2회 실행 모두 재현, 가장 심각)**

검색은 정답 문서(위치정보약관 제8조, "6개월간")를 정확히 1위로 찾았지만, 함께 딸려온 충돌 문서(통합약관 제16조, "6개월 **이상**")의 표현을 답변에 섞어 넣어 **"6개월 이상"이라고 오답**을 냈다. 심지어 "확실히 알 수 있습니다"라며 확신에 찬 어조로 틀렸다.

**P08 — 근거는 맞게 인용하고 결론만 반대로 뒤집음 (실제 제출 2회 모두 재현)**

정답은 "아니오, 세부지침에 따릅니다"인데, 모델이 근거 원문("세부지침에 따릅니다")을 정확히 인용해놓고 바로 다음 문장에서 "본 약관이 우선 적용된다"고 결론을 반대로 냄. 자기모순이 텍스트 안에 그대로 드러남.

**인용 조항 번호 조작**

- P10: `(근거 1 제②조)` 처럼 존재하지 않는 조번호 표기(①②③은 항 번호이지 조 번호가 아님)
- T04: 검색은 제9조를 정확히 썼는데 인용 문구는 "제4조 제1항 7호"라는 무관한 번호를 지어냄

**할루시네이션·품질 저하 (재실행마다 다른 새 패턴으로 발생 — 불안정성 자체가 문제)**

- T01: "배상**责任**" — 한글 문장에 한자 누출
- T03 (2차 실행에서 신규 발생): 무관한 조항(위치기반서비스 제16조)을 억지로 끌어와 "다시 한번 확인할 수 있습니다"라고 근거 없는 확신을 추가
- T04 (2차 실행에서 신규 발생): "카카오계정 **�**약관" — 인코딩 깨짐(mojibake)
- 전체 10문항 공통: `(문서명 제N조)` 리터럴이 실제 문서명으로 치환 안 되고 그대로 출력됨

**`do_sample=False`인데도 재현 안 되는 오류가 있다는 점**이 중요하다 — 같은 코드를 두 번 돌렸을 때 T01·T02·T05·T06은 완전히 동일했지만 T03·T04는 서로 다른 새로운 실패 패턴을 냈다. 버그를 하나씩 땜빵하기보다 프롬프트 자체를 방어적으로 재설계해야 한다는 근거.

### 8-4. 결론

**검색·청킹은 이 시점부로 검증 완료로 간주한다.** 원 공개 10문항, 패러프레이즈, 애매한 질문, 문서명 명시 질문, 자체 제작 함정 10문항, 72개 조항 전수까지 — 로컬 테스트와 Colab 실전 실행 모두에서 검색 MRR은 예외 없이 1.000에 가까웠다(§8-2b). 남은 작업은 생성 프롬프트 쪽이었고, §9에서 처리했다.

---

## 9. 생성 프롬프트 수정 (2026-08-10)

§8-3에서 확정된 버그들을 근거로 시스템 프롬프트를 재설계하고, 프롬프트만으로 100% 방지가 안 되는 패턴(재실행마다 다르게 실패하는 걸 §8-3에서 확인)에 대비해 후처리 정규식 안전장치를 추가했다.

### 9-1. 시스템 프롬프트 변경 (2026-08-10, 사용자 초안과 병합해 최종 확정)

1차 버전(9개 규칙) 이후, 사용자가 별도로 작성한 프롬프트 초안(완결성·정확한 세부정보 유지·예/아니오 결론 우선·N개 항목 요구 대응·간결성 등을 담은 버전)을 받아 병합해 10개 규칙의 최종본으로 확정했다.

가장 근본적인 변경은 여전히 **인용 방식**이다. 기존엔 "'(문서명 제N조)' 형식으로 표시하세요"라는 추상적 지시를 줬는데, 3B 모델이 "문서명"을 채워야 할 변수가 아니라 문자 그대로 출력할 리터럴로 이해했다(모든 리터럴 버그·인용번호 조작의 근본 원인). 규칙 9번은 모델이 이미 자연스럽게 잘 쓰고 있던 `[근거 1]`, `[근거 2]` 표시를 그대로 재사용하게 하고, 처음부터 "문서명+제N조"를 새로 조합하는 걸 금지한다 — 조합하려다 실패하는 지점 자체를 없앴다.

**병합 시 가장 중요했던 판단은 규칙 6번이다.** 사용자 초안의 원래 문구("서로 다른 근거가 동일/관련 내용을 규정하면 종합하여 답하라")를 그대로 쓰면 T05(문서 간 수치 충돌: "6개월" vs "6개월 이상")에서 오히려 "종합하라"는 지시가 두 표현을 섞으라고 부추기는 셈이 된다 — 실제로 모델이 그렇게 틀렸던 바로 그 실패를 재현할 위험이 있었다. **"보완적으로 규정하는 경우(종합)"와 "충돌하는 경우([근거 1] 우선, 섞지 않음)"를 명시적으로 분리**해 P02류(3개 문서가 일치하는 내용이라 종합해도 되는 경우)는 살리고 T05류는 막았다.

| 규칙 | 대상 버그 |
|---|---|
| 1, 4 (완결성, N개 항목) | P03·P04류 답변 누락 |
| 2 (세부정보 정확 일치) | exact형 문항 전반 |
| 3 (예/아니오 결론 우선 + 결론 뒤집기 금지) | P08 자기모순 — 결론을 먼저 못박아 구조적으로 방지 강화 |
| 5 ([근거] 없는 조항 번호·사실 창작 금지) | T04, P10 인용 조작 |
| 6 (보완 시 종합 / **충돌 시 [근거 1] 우선, 섞지 않음**) | T05 문서 충돌 — 사용자 초안의 "종합" 지시가 오히려 위험할 뻔한 지점을 분리해 방지 |
| 7 (긴 근거도 핵심 누락 금지) | truncation 제거(5-4)를 프롬프트로도 재강조 |
| 8 (간결·직접, 작성 과정 설명 금지) | 메타 발언 섞임 |
| 9 (`[근거 N]` 표시만 재사용, 새로 조합 금지) | 전체 "문서명" 리터럴 버그 |
| 10 (한국어 전용) | T01 한자 누출 |

### 9-2. 후처리 안전장치 (`_postprocess_answer`)

프롬프트 지시만으로는 재현이 불안정하다는 걸 §8-3에서 이미 확인했으므로(같은 코드 2회 실행에서 T03·T04가 매번 다르게 실패), 생성 후 알려진 버그 패턴을 정규식으로 한 번 더 청소한다:

- `(문서명 ...)` 류 리터럴 잔존 시 제거
- "이 답변에서는/따라서 답변에서는"으로 시작하는 메타 발언 문단 제거
- 위 제거로 남는 덜렁 쉼표·중복 공백·빈 줄 정리

`answer_question()`이 `_generate()` 결과를 `_postprocess_answer()`에 통과시킨 뒤 반환하도록 변경. 실제 버그 샘플 4종(팀 8 제출 답변에서 그대로 추출)으로 테스트해 전부 깨끗하게 제거되는 것 확인, 정상 텍스트는 안 건드리는 것도 확인.

### 9-3. 검증

- 두 셀 모두 재컴파일 확인
- `_postprocess_answer`를 노트북에서 직접 추출해 실행 — 실제 버그 샘플 3종 전부 정상 제거
- 검색 회귀 테스트: 공개 10문항 MRR = 1.000 유지(생성 쪽 변경이 검색에 영향 없음 재확인)

### 9-4. 실전 반복 검증 (2026-08-10, 제출 버전 (4)→(5)→(6))

실제 Colab 제출을 3회 더 받아 MRR·F1(근사)·정성 품질을 매번 같은 방식으로 재확인했다.

| | (4) | (5) | (6) |
|---|---|---|---|
| MRR+F1 확정 소계(50점 만점) | 36.06 | 37.36 | 37.90 |
| LLM판정 추정(50점 만점, 실제 Gemini 아님) | 30.9 | 35.8 | 37.65 |
| 추정 총점 | ≈67 | ≈73 | ≈75.5 |

리터럴 버그·메타발언·불완전 답변은 꾸준히 개선됐으나, **P08(자기모순)만 세 번 모두 그대로 재현**됐다 — 규칙 문구를 세 가지 다른 방식으로 고쳐봐도 "충돌하면 세부지침에 따른다"는 근거를 인용해놓고 결론은 계속 반대로 냈다. 규칙 설명만으로는 3B 모델이 이 패턴을 못 넘는다는 뜻으로 판단.

### 9-5. 실제 채점 피드백 기반 2차 수정 (2026-08-10)

사용자가 실제 LLM 채점 피드백(감점 사유) 5건을 제공해, 그동안의 F1 분석·정성 평가로 추정만 하던 원인들을 실측으로 확정했다.

| 감점 사유 | 대응 |
|---|---|
| P02: "최대한 빠른 시일 내 복구 노력" 등 부가 설명 누락 | 규칙 1에 "결론뿐 아니라 조건·절차·노력 여부 등 관련 세부 설명도 포함" 추가 |
| P04: "무엇을 어떤 방법으로"에서 방법만 답하고 무엇을은 누락 | 규칙 1에 "질문이 여러 요소를 동시에 물으면 전부 답하라" 명시 |
| P08: 근거를 정확히 인용하고 결론만 반대로 냄 (치명적) | **few-shot 예시 추가** — 실제 문항이 아닌 가상의 "요금정책 7일 이내 해지" 조항으로 같은 "원칙 vs 예외로 결론이 뒤집히는" 구조를 실제 user/assistant 대화 턴으로 시스템 프롬프트 뒤에 삽입(`_FEWSHOT_USER`/`_FEWSHOT_ASSISTANT`, `_generate()`에서 실제 질문 앞에 삽입) |
| P09: 핵심 키팩트 누락 + 안 묻은 내용 추가로 정밀도 하락 | 규칙 5에 "질문이 묻지 않은 내용을 추가로 덧붙이지 마세요" 명시 |
| P10: 내용은 맞았으나 `(근거 1 참조)`가 문장마다 노출돼 명료성 감점 | **규칙 9를 "본문 인용 표시 전면 금지"로 전환** — `[근거 N]` 재사용 허용에서 아예 금지로 강화. 근거 표기는 이미 `retrieved` 필드가 코드로 정확하게 담당하므로 본문에서 또 표기할 필요가 없다고 판단 |

**인용 형식 버그의 역사**: `(문서명 제N조)` 리터럴 → `제②조` 같은 존재하지 않는 조번호 → `제4조 제1항 7호` 지어내기 → `[근거1]` 잔존 → `(근거 1 참조)` 반복 노출까지, 매번 다른 방식으로 재발했다. 근본 원인은 "본문에 인용을 표기하라"는 요구 자체였다고 결론짓고 이번에 요구 자체를 없앴다.

`_postprocess_answer`도 강화해 `[근거 N]`, `(근거 N 참조)`, `제①조`류 표기를 전부 정규식으로 제거하도록 확장(안전장치, 프롬프트 실패 시 최종 방어선).

**검증**: 두 셀 재컴파일, 검색 MRR 1.000 유지, few-shot 메시지 렌더링 확인, 강화된 후처리 정규식을 실제 버그 샘플(P10의 `(근거 1 참조)` 반복, `(근거 1 제②조)`, `[근거 1]` 잔존, `(문서명 제N조)`) 전부로 테스트해 정상 제거 확인.

### 9-6. few-shot 라벨 편향 보정 (2026-08-10)

사용자 지적: 예시가 하나뿐이고 정답이 항상 "아니오"면, 모델이 근거를 실제로 판단하지 않고 "이 질문 유형엔 아니오"라는 표면 패턴만 외울 위험이 있다(라벨 편향) — 그러면 진짜 "네"가 맞는 질문에서 없는 예외를 지어내는 새 오류가 생길 수 있다. 타당한 지적으로 판단해 즉시 반영.

`_FEWSHOT_USER`/`_FEWSHOT_ASSISTANT` 단일 쌍을 `_FEWSHOT_PAIRS` 리스트로 교체하고, "예외로 뒤집히는 경우(아니오)"에 "전제가 그대로 맞는 경우(네)"를 짝으로 추가했다:

- 예시 1(아니오): 요금정책 "원칙+다만 예외" 구조, 질문의 "예외 없이 항상"이라는 전제를 근거가 뒤집음
- 예시 2(네): 이용계약 해지 조항, 질문의 전제가 근거와 그대로 일치 — 여기서 괜히 예외를 지어내면 안 된다는 것도 함께 학습

`_generate()`는 `_FEWSHOT_PAIRS`를 순회하며 각 쌍을 user/assistant 턴으로 추가하도록 수정. 두 셀 재컴파일, 두 예시 정상 렌더링, 검색 MRR 1.000 유지 확인.

### 9-7. 실전 검증 결과 — 제출 (7): few-shot 순이익 없음, 새 회귀 발생 → 되돌림 (2026-08-10)

실제 Colab 제출 (7)을 확인한 결과(팀 협업 과정에서 시스템 프롬프트 자체도 12개 규칙으로 더 정교하게 재작성된 상태였음 — `[답변 만드는 법]/[사실 여부를 묻는 질문]/[근거가 여러 개일 때]/[형식]` 4개 절로 구조화, "원문 문장을 그대로 옮겨 쓴다"는 축어적 인용 원칙 도입):

- **P08은 4번째도 여전히 오답** ("네, ... 본 약관이 세부지침보다 우선" — 정답은 "아니오")
- **P09에서 새 회귀 발생**: (4)(5)(6) 전부 핵심 결론("라이선스는 탈퇴 후에도 존속")을 맞혔는데, (7)에서 처음으로 "라이선스의 효력은 종료됩니다"로 반대로 뒤집힘. 근처에 있는 무관한 예외 문구("일부 서비스는 게시물 삭제 불가능할 수도 있음")를 엉뚱하게 라이선스 존속 여부에 잘못 적용한 것으로 추정
- MRR+F1 확정소계도 37.90(6) → 36.44(7)로 하락

**결론**: few-shot이 겨냥한 버그(P08)는 못 고치면서 다른 곳(P09)에 새 부작용을 만들어 순이익이 마이너스. 사용자 지적대로 "특정 예시가 들어가면 일반화가 깨진다"는 우려가 실제로 확인된 사례. **되돌림.** `_generate()`를 시스템 프롬프트+질문만 쓰는 구조로 원복하고 `_FEWSHOT_PAIRS` 코드 제거.

### 9-8. 검색 임계값을 절대값 → 상대비율로 전환 (2026-08-10)

되돌리기와 별개로, 사용자가 "검색이 항상 4개를 가져오는 것"을 손볼 필요가 있는지 질문 — 이전에 발견했던 사실(§8-3 인접 논의)과 연결: 실제 제출 파일들에서 10문항 전부 항상 정확히 4개가 반환되고 있었고, 이는 `rel_threshold=0.3`이 한 번도 걸러낸 적 없는 죽은 파라미터였기 때문이다. 원인은 RRF 융합 점수가 순위 기반이라 1~4위 점수 차이가 수학적으로 작게 나오는 구조(94~100%대로 압축)라서, 절대 임계값 0.3보다 항상 훨씬 높게 나왔던 것.

`_retrieve()`의 `rel_threshold`(절대값)를 `rel_gap_ratio=0.7`(1위 점수 대비 상대 비율)로 교체 — 리랭커 점수든 RRF 폴백 점수든 스케일이 달라도 일관되게 "1위와 확실히 차이 나는 후보만 제외"하도록 함.

**한계**: 로컬(리랭커 없이 RRF 폴백)로 재검증한 결과 여전히 10문항 전부 4개가 반환됨 — RRF 자체의 점수 압축 때문에 상대비율 0.7로도 못 거른다. **리랭커가 실제로 붙는 Colab 경로에서는 sigmoid 점수가 의미 있는 격차를 만들어 효과가 있을 것으로 예상되나, GPU가 없어 로컬에서는 검증 불가 — Colab에서만 확인 가능.** 검색 MRR은 1.000 유지(회귀 없음).

### 9-9. 다음 확인 사항

**Colab에서 재실행해 확인할 것 두 가지**: (1) few-shot 되돌린 뒤 P08~P10이 (6) 수준으로 돌아왔는지(특히 P09 회귀가 사라졌는지), (2) 상대비율 임계값(0.7)이 리랭커 경로에서 실제로 4개 미만을 반환하는 경우가 생기는지, 그리고 그게 T05류 문서 충돌 오염을 줄이는 데 도움이 되는지.

---

## 10. 프롬프트 설계 근거의 수치화 + 규칙 6·7 재설계 (2026-08-10)

§9-7까지는 "관측된 버그를 규칙 문구로 막는" 방식이었고, 12규칙 프롬프트(제출 (7))도 그 연장선이었다. 여기서는 방향을 바꿔 **채점지표에서 거꾸로 이상적인 답변 형태를 역산**하고, (7)의 실패를 그 틀로 재해석했다.

재현 스크립트: [`하네스/f1_style_sim.py`](하네스/f1_style_sim.py)

### 10-1. key_facts의 정답 단위는 "조"도 "항"도 아닌 **문장**

공개 골드셋 key_facts를 원문과 1:1 대조한 결과, key_facts는 **정답을 담은 원문 문장 하나를 의미 단위로 쪼갠 것**이었다.

| 문항 | key_facts | 원문 대응 |
|---|---|---|
| P02 | 2개 | 제8조 ②항의 **마지막 한 문장**("…복구하도록 노력하되, 2시간 이상 복구가 지연될 시 …알려 드리겠습니다") |
| P04 | 2개 | 제6조 ③항의 **한 문장 전체** |
| P10 | 3개 | 질문이 3가지를 물어 ①②③항에서 각 한 문장씩 |

→ §9-5에서 서로 다른 규칙으로 따로 대응했던 P02 감점("복구 노력 누락")과 P04 감점("무엇을 누락")은 실은 **같은 원인 하나**다: 정답 문장을 통째로 옮기지 않고 뒷부분만 잘라 썼다.

### 10-2. 답변 스타일별 F1 실측 — 봉우리가 좁다

공식 토크나이저가 비공개라 두 근사(조사 제거 어절 / 문자 bigram)로 교차 확인했다. 두 방식의 순위가 일치해 결론은 토크나이저 선택에 둔감하다.

| 답변 스타일 (P02) | 어절 F1 | bigram F1 |
|---|---|---|
| 결론만 ("2시간입니다") | 0.190 | 0.148 |
| 질문 복원 + 결론 | 0.519 | 0.466 |
| **정답 문장 통째 복사** | **0.829** | **0.792** |
| 항(②) 전체 복사 | 0.436 | 0.401 |
| 조 전체 복사 | 0.409 | 0.385 |
| 3개 문서 중복 종합(T05형) | 0.600 | 0.580 |
| 메타발언+인용표기 포함 | 0.348 | 0.393 |

**덜 써도(0.19) 더 써도(0.41) 급락한다.** 규칙 1·2("문장 단위로, 통째로, 원문 표현 그대로")의 근거.

여기서 파생된 판단 둘:

- ~~**본문 인용 표기는 넣지 않는 게 맞다 — §9-5 판단을 수치로 확정.** 코드가 정확한 문서명·조번호를 앞에 붙이는 안(할루시네이션 위험 0)조차 F1 0.829 → 0.694(**−0.135**, 30점 환산 약 −4점). 만회하려면 근거성(12.5점)에서 30%p 이상 올라야 한다. `retrieved` 필드가 이미 근거를 정확히 전달하므로 기각.~~ **→ §15에서 뒤집힘(2026-08-12).** 이 −0.135는 *문서명+조번호를 문장에 서술*하는 무거운 형태의 비용이었다. 공식 채점 결과 데이터를 보니 판정 심판이 **근거성을 본문 조항 표기로 매기고 있어서**, `(제N조)` **최소 형태**로 넣기로 결정을 뒤집었다. 최소 형태의 F1 비용은 −0.0147(30점 환산 −0.44점)로 1/9이고, `retrieved` 필드는 심판의 근거성 축이 참조하지 않는다. 상세는 §15.
- **`max_new_tokens` 320 → 640.** P03형 나열 질문은 key_facts만 480자(추정 320~480토큰)다. 항목 이름만 나열하면 F1 **0.330**, 정의까지 쓰면 **1.000**인데 320토큰으로는 정의를 다 쓸 수 없다 — 프롬프트로 "전부 쓰라"고 해도 물리적으로 잘린다. 문항당 타임아웃 120초에 현 p50 17초라 여유 충분.

### 10-3. (7)의 P08 실패는 형태가 바뀌었다 — 규칙 6이 원인

P08의 실패 형태 변화가 진단의 열쇠다.

| | 실패 위치 |
|---|---|
| (4)(5)(6) | 근거를 옳게 인용한 뒤 **마지막 "따라서~" 요약 문장**에서 뒤집힘 |
| **(7)** | `"네, ... 본 약관이 세부지침보다 우선"` — **첫 토큰부터** 틀림 |

규칙 8이 노렸던 "따라서 슬롯"은 실제로 제거됐다. 그런데 규칙 6이 판정어를 맨 앞에 강제하는 바람에 **모델이 근거를 옮겨 적기 전에 네/아니오를 확정**해야 했고, 버그가 그 자리로 이동했다.

**앞선 판단 정정**: 12규칙 작성 시 "판정 먼저가 F1에 유리(0.909 vs 0.833)"로 보고 판정 먼저를 택했는데, 그 비교는 순서가 아니라 **내용이 달랐던** 비교였다(뒤쪽 버전에 "따라서~" 요약문이 덤으로 붙어 있었음). 토큰 내용을 동일하게 두고 위치만 바꾸면:

| | 어절 F1 | bigram F1 |
|---|---|---|
| `아니오, ` + 근거문장 | 0.909 | 0.860 |
| 근거문장 + 끝에 `아니오.` | **0.909** | **0.872** |

F1은 bag-of-tokens라 **판정어 위치 비용이 0**이다. 근거 먼저로 바꾸는 데 F1 손해가 없다.

### 10-4. P09 회귀의 원인 귀속 재검토 — few-shot보다 규칙 7이 유력

§9-7은 P09 회귀를 few-shot 탓으로 보고 few-shot만 되돌렸다. 그런데 §9-7 자신의 기술("근처의 무관한 예외 문구를 라이선스 존속 여부에 잘못 적용")은 **규칙 7이 시키는 동작 그 자체**다 — "단정 표현이 있으면 예외를 찾아 아니오로 뒤집어라". P09는 단정형 질문도 아니라 규칙 7이 발동하면 안 되지만, 3B 모델은 규칙을 그렇게 정밀하게 게이팅하지 않는다.

**(7)은 세 가지(12규칙 전면 개편 · few-shot · 640토큰)를 동시에 바꿔서 원인 귀속이 불가능한 실험이었다.** few-shot만 되돌린 상태로는 규칙 7이 남아 P09가 재현될 수 있다.

### 10-5. 조치 — 규칙 6·7 교체 (규칙 7 삭제)

```
6. 예/아니오로 답할 수 있는 질문은, 먼저 그 쟁점을 직접 규정한 근거 문장을
   그대로 옮겨 쓴 다음, 맨 마지막에 '네.' 또는 '아니오.' 한 단어만 덧붙입니다.
7. 마지막의 '네'/'아니오'는 바로 앞에 옮겨 쓴 문장이 말하는 대로만 정합니다.
   옮겨 쓴 문장과 반대되는 판단을 쓰지 않습니다.
8. (유지) '따라서/그러므로/결론적으로' 요약 문장을 새로 만들지 않습니다.
```

기존 규칙 7("예외를 찾아 뒤집어라")은 삭제했다. 예외절은 **규칙 2(문장 전체를 통째로 옮김)가 '다만' 절까지 자동으로 끌고 오므로** 별도 지시 없이 커버된다는 판단이다.

**감수한 위험**: 자체 함정 세트 T01~T10이 전부 "예외 없이 항상" 전제 뒤집기라, 규칙 7은 원래 그쪽을 겨냥한 것이었다. 규칙 7 삭제가 T계열 성능을 떨어뜨리는지 확인이 필요하다.

### 10-6. 다음 확인 사항

Colab 재실행으로 (a) P08이 드디어 정답을 내는지, (b) P09 회귀가 사라지는지(사라지면 원인은 규칙 7 또는 few-shot — 둘 다 빠진 상태), (c) T01~T10 함정 세트가 규칙 7 없이도 유지되는지, (d) P03이 640토큰 안에서 5개 항목 정의를 다 쓰는지.

---

## 11. 리랭커 입력 truncation 버그 — 발견 및 해결 (2026-08-10)

### 11-1. 문제 발견

`_retrieve()`가 리랭커에 넘기는 텍스트를 `_CORPUS_TEXT[i][:1000]`으로 자르고, `CrossEncoder(..., max_length=512)`로 한 번 더 잘랐다. §5-4에서 LLM 프롬프트 쪽 1000자 truncation은 이미 제거했지만, **리랭커 입력 쪽은 그때 안 건드렸다** — 당시 "채점(순위)에만 쓰이고 답변 생성엔 안 들어가니 정보 손실과 무관"이라 판단했는데, 이 판단이 틀렸다: 리랭커가 못 보면 애초에 정답이 `retrieved` 상위 4개에 못 들어가므로 LLM이 볼 기회 자체가 없다.

**정량화**: 실제 `bge-reranker-v2-m3` 토크나이저로 72개 조항 전수 측정 — 8개(11%)가 `[:1000]` 후에도 512토큰을 넘겨 뒷부분이 사라짐. 최악: 통합서비스약관 제12조(금지행위 목록, 3482자) — **12번 항목(원문 1042번째 글자)부터 23번 항목까지, 2482자 분량이 통째로 사라짐.** 같은 조항·같은 질문으로 "조 전체(1000자 truncate)" 리랭커 점수 0.03 vs "그 안의 실제 관련 항목만" 0.85 — 31배 차이로 메커니즘 실증.

### 11-2. 두 가지 수정안을 실측으로 비교

공개 10문항 + 자체 함정 10문항(§8-2) + 신규 프로브 3문항(1000자 뒤에 정답 단서를 일부러 묻어 자체 제작)으로 세 방식을 동일 조건 비교(로컬 CPU, 실제 `bge-reranker-v2-m3` 그대로 사용, GPU 없어 속도 절대값은 참고용).

| 방식 | 공개 10 MRR | 함정 10 MRR | 종합 20 MRR |
|---|---|---|---|
| 현행 (`[:1000]` + `max_length=512`) | 1.0000 | 0.9000 (T01·T09 둘 다 2위) | 0.9500 |
| 안 A: 서브청크(항 단위) 리랭커 점수 + 조 전체 점수를 RRF 결합 (BM25 §2-1과 동일 발상) | 1.0000 | 0.9000 (변화 없음) | 0.9500 |
| **안 B: truncation 완전 제거 + `max_length=2048`** | 1.0000 | **0.9500 (T09 회복)** | **0.9750** |

**안 A(서브청크 결합)는 20문항 + 프로브 3문항 전부에서 현행과 순위가 1도 다르지 않았다** — 구현 복잡도만 늘고 효과가 없었다. 이유를 프로브 결과로 확인: 리랭커는 "특정 항목 문장을 읽어서" 맞히는 게 아니라 "제목+앞부분 항목들만 봐도 이 조항이 그 주제(금지행위 나열 등)구나"라는 **주제 수준 매칭**만으로 이미 맞히고 있었다 — 그래서 1000자 안에 담긴 제목·앞부분 몇 항목만으로도 정답을 골라내는 데는 지장이 없었다(단, 안전마진은 위험할 정도로 좁다 — §11-3 참조).

**안 B(제한 완전 해제)가 유일하게 실제 개선을 냈다** — T09(카카오 통합 약관 제10조, 라이선스 존속 조항)가 2위에서 1위로 회복됐다. 게다가 더 단순하다.

**속도**: pool 30개 전체 리랭킹 기준 `max_length=512`+1000자truncate = 9.91초, `max_length=2048`+truncation 없음 = **7.47초로 오히려 빠름**(CPU, 배치 패딩 효과로 추정 — sentence-transformers가 배치 내 최대 길이로만 패딩하지 `max_length` 설정만큼 항상 패딩하진 않음). GPU에서도 재확인 필요하지만 최소한 "느려질 것"이라는 우려로 반대할 근거는 약하다.

### 11-3. 안 A(서브청크 결합)를 폐기하되, 확인된 위험은 기록해 둔다

안 A는 채택하지 않지만, 검증 과정에서 확인한 사실 하나는 남긴다: 서브청크 점수를 **단독으로**(결합 없이) 쓰면 위험하다는 게 페어 비교로 확인됐다 — 정답 vs 보일러플레이트 오답 격차가 "조 전체만" 0.97에서 "서브청크 최고점만" 0.15로 84% 붕괴(§ 이전 대화 실측). 이는 BM25 §2-1에서 이미 배운 교훈("짧은 항 단독 스코어링은 문맥 없이 과대평가됨")이 리랭커에도 그대로 적용된다는 뜻이다. 안 A(RRF 결합)는 이 위험을 피하는 안전한 형태였지만, 그럼에도 효과가 없어 채택 안 함 — "단독 서브청크"는 여전히 하지 말아야 할 방식으로 남겨둔다.

### 11-4. 조치

`결과기_팀구현.ipynb` 1번 셀 두 곳 수정:
- `CrossEncoder(..., max_length=512)` → `max_length=2048` (코퍼스 최장 조항이 1683토큰이라 여유 포함 2048로 설정, 토크나이저 자체 한도 8192 안에서 충분)
- `pairs = [(question, _CORPUS_TEXT[i][:1000]) for i in pool]` → truncation 제거, 조항 원문 전체 그대로 전달

두 셀 재컴파일 확인.

### 11-5. 다음 확인 사항

Colab GPU에서 재검증 필요: (a) 실제 T4에서도 속도 저하가 없는지(로컬 CPU 실측은 오히려 빨랐지만 GPU 배치 처리 특성이 다를 수 있음), (b) 문항당 120초 타임아웃 안에서 여유가 유지되는지, (c) T09 회복이 실제 파이프라인(BM25+리랭커 전체)에서도 재현되는지.

---

## 12. 생성 모델 7B-4bit vs 3B-fp16 A/B 실측 (2026-08-11)

§1~11은 검색·프롬프트만 다뤘고 생성 모델 교체는 기록이 없었다. Colab T4에서 처음으로 실측했다.

### 12-1. 결과 — 통계적으로 미결

검색·리랭커·프롬프트·후처리 전부 동일하게 두고 생성 모델만 교체(5번 셀). 공개 10문항, bigram/어절 두 가지 F1 근사.

| | 7B-4bit | 3B-fp16 |
|---|---|---|
| 평균 bigram F1 | **0.671** | 0.637 |
| 평균 어절 F1 | **0.638** | 0.612 |
| 문항 승패(bigram) | 6 | 4 |
| 평균 생성 지연 | 7.8s | 6.2s |

**차이 +0.034는 의미 없다.** 10문항 대응표본 t검정:

| 지표 | 평균 차이 | t | 95% 신뢰구간 |
|---|---:|---:|---|
| bigram | +0.034 | 0.82 | **[−0.060, +0.129]** |
| 어절 | +0.026 | 0.45 | **[−0.104, +0.155]** |

두 지표 다 신뢰구간이 0을 포함하고 "3B가 0.06 낫다"까지 포함한다(30점 환산 [−1.8점, +3.9점]). 6승 4패도 동전 던지기와 구분되지 않는다(p≈0.38).

**게다가 우위가 두 문항에 얹혀 있다.**

```
P06 +0.313
P09 +0.183
─────────────────────
나머지 8문항 평균  -0.019   ← 3B 우세
전체 평균         +0.034
```

P06·P09를 빼면 3B가 8문항에서 앞선다.

### 12-2. 그런데 그 두 문항의 차이는 모델 크기 문제가 아니다

- **P06**: 3B가 질문 표현을 그대로 되돌려줬다("동의, 확인, 승낙의 순서로 체결됩니다"). 7B는 원문을 인용했다("본 약관의 내용에 동의한 후 회사가 카카오계정 정보 등을 확인한 후 승낙함으로써"). gold key_fact가 원문 표현이라 7B가 이겼다 — **규칙 2(원문 표현 유지) 미준수**이지 이해력 차이가 아니다.
- **P09**: 3B는 kf1을 맞췄으나("탈퇴한 후에도 … 존속하게 됩니다" — gold와 사실상 동일) 뒤에 서브라이선스 얘기 3문장을 덧붙여 precision을 깎았고 `또한, 서비스의`에서 잘렸다. `_generation_budget()`상 P09는 분해도 목록형도 아니라 **200토큰**을 받는데 거기서 끊긴 것. **과잉 생성 문제**다.

### 12-3. 판정

**"7B로 올려서 좋아졌다"고 말할 근거는 없다.** 발표자료에 그렇게 쓰면 신뢰구간 질문 하나에 무너진다.

다만 **7B를 켜두는 것 자체는 손해가 아니다** — 리스크가 비대칭이다. bitsandbytes 준비나 4bit 로딩이 실패하면 `_load_generation_model()`이 조용히 3B로 폴백하는데, 그게 바로 비교 대상이다. downside가 "3B"고 upside가 "아마 조금 나음"이라 켜두는 게 합리적이다. 지연 7.8s도 문항당 120초 제한에 여유가 크다.

정확한 표현은 **"이득이 실측으로 확인됐다"가 아니라 "손해 아님이 확인됐다"**이다.

### 12-4. 7B-4bit 쪽 경고 신호 (n=1, 추적 필요)

P07에서 7B가 오히려 퇴보했다(−0.127):

> [7B-4bit] "…포함되지 않는 서비스는 카카오 계열사가 제공하는 서비스입니다. 예로, 카카오 T택시 서비스가 **들립니다**."
> [3B-fp16] "…**회사가 아닌** 카카오 계열사에서 제공하는 서비스입니다. 약관은 예로, 카카오 T택시 서비스를 **들고 있습니다**."

gold key_fact의 핵심 한정어 `회사가 아닌`이 빠졌고 `들립니다`는 비문이다. NF4 양자화 품질 저하의 전형적 형태라 본선 전 P07을 따로 확인할 것.

### 12-5. 두 모델이 같은 곳에서 실패한다 — 남은 헤드룸은 모델 밖에 있다

하위 4개가 완전히 겹친다: P02(0.47/0.44), P08(0.44/0.51), P07(0.50/0.63), P09(0.52/0.34).

특히 **P09에서 두 모델 다 kf2("전 세계적이고 영구적인 라이선스")를 언급하지 않는다.** 7B는 간결해서, 3B는 딴 얘기 하다 잘려서 — 이유는 다른데 결과가 같다.

**구조축약 손실 가설은 검증 결과 기각됐다.** 통합 약관 제10조(1391자)를 `_split_article_structural_blocks()`에 실제로 넣어보니 번호 항목 5블록으로 나뉘고, **kf1(538자)과 kf2(241자)가 같은 블록 [1](561자) 안에 함께 있다.** 그 블록이 anchor로 잡히면 561자가 통째로 들어가므로 축약이 kf2를 잘라낼 수 없다.

즉 P09 kf2 누락은 **순수 생성 문제**다. 질문이 "라이선스의 효력은 어떻게 되나요"이므로 모델 입장에선 "존속한다"로 답을 마친 것이 질문에 충실한 행동이고, gold가 라이선스의 성격까지 요구하는 것이 더 넓다. **규칙 1("같은 규정을 완성하는 권리·후속 사실도 빠뜨리지 마세요")이 커버해야 할 지점인데 3B·7B 둘 다 못 따르고 있다.**

MRR은 1.000으로 이미 천장이고 key_fact 커버리지는 65.4%다. 남은 헤드룸 전부가 검색 아래에 있다.

---

## 13. §11-4 조치 미반영 발견 (2026-08-11)

§12 분석 중 설계노트 §11의 결정과 현재 코드가 어긋나 있는 것을 확인했다.

| §11 결정 | `결과기_팀구현.ipynb` 1번 셀 현재 상태 |
|---|---|
| `max_length=512` → **`2048`** (§11-4) | **`max_length=512`** — 미반영 |
| `[:1000]` truncation 제거 (§11-4) | `_CORPUS_TEXT[i]` — 반영됨 |
| 안 A(서브청크 RRF 결합) **폐기** (§11-3) | `_best_subchunk_for_question()` 채널 2로 **채택돼 있음** — 결정과 반대 |

### 13-1. 영향

§11-1 실측대로 `[:1000]`을 지워도 `max_length=512`가 남아 있으면 **512토큰(≈1059자)에서 여전히 잘린다**. 코퍼스 72개 중 8개(11%)가 여기 걸리고, 최악은 통합서비스약관 제12조(금지행위 목록, 3482자=1683토큰) — **2482자가 리랭커 채널 1에서 사라진다.**

공개 10문항에는 영향이 없다(P09의 key_fact도 241·538자로 컷 앞이다). 하지만 **본선 30문항이 제12조를 물으면 바로 걸린다.** 현재는 채널 2(서브청크)가 우연히 메워주고 있는데, §11-3이 "서브청크 단독은 정답 vs 보일러플레이트 격차가 84% 붕괴"라고 경고해둔 바로 그 채널이다.

### 13-2. §11-2와 현재 docstring이 모순되지 않는 이유

§11-2는 "안 A는 순위를 1도 못 바꿨다"고 했고, 현재 `_retrieve` docstring은 "2채널 RRF로 종합 MRR 0.9750→1.0000"이라고 한다. **기준선이 달라서 둘 다 맞다** — §11-2는 `[:1000]`+512 위에서 쟀고, 현재 코드는 truncation 제거+512 위에서 쟀다.

**다만 `max_length=2048` + 안 A 조합은 아직 측정된 적이 없다.** 실험 매트릭스에서 비어 있는 칸이다.

### 13-3. 다음 확인 사항

- `max_length=2048`로 올리고 20문항(공개 10 + 함정 10) MRR 재측정 — 회귀 없으면 채택. §11-2에서 속도는 오히려 빨랐다(CPU 9.91s → 7.47s).
- 설계노트 §11-3의 "안 A 폐기" 기술을 현재 채택 상태에 맞게 정정할 것.

---

## 14. 공식 채점 결과 역산 → keyfact F1 개선 (2026-08-11)

공개 10문항 공식 결과(`practice_result_8.json`)를 받았다. **MRR 1.000 / keyfact_f1 0.6551 / judge 97.45 / 총점 88.378.**
MRR과 judge는 사실상 천장이고, 남은 점수의 거의 전부가 F1(가중치 0.3)에 있다.

### 14-1. 채점식 역산 — 공식 keyfact_f1은 bag-of-tokens F1이다

per_question 10건을 여러 후보식과 대조해 다음을 확인했다.

| 후보식 | 공식 점수와의 MSE |
|---|---|
| 답변 전체 vs key_facts 이어붙인 문자열, **어절 multiset F1** | **0.0022** |
| 같은 방식, 어절 **집합(set)** F1 | **0.0015** |
| 문자 bigram F1 | 0.0038 |
| key_fact별 F1의 평균 | 0.07~0.10 (불일치) |

즉 **답변 전체를 하나의 토큰 가방으로 보고 key_facts 전체와 F1을 잰다.** 세 가지 귀결이 나온다.

1. **누락과 군더더기가 같은 무게로 깎인다.** 리콜만 보던 4번 셀로는 절반만 보였다.
2. **어미·조사가 다르면 다른 토큰이다.** `금지되어 있으며`(답변) vs `금지됩니다`(정답)는 양쪽에서 1토큰씩 손해다. 원문 표현 유지가 곧 점수다.
3. **순서는 비용 0이다** — §10-3 결론과 일치.

### 14-2. 손실의 정체 — 문항별 P/R 분해

| 문항 | F1 | P | R | 원인 |
|---|---|---|---|---|
| P07 | 0.333 | 0.44 | 0.21 | key_fact 3개 중 2개 누락(`회사가 아닌`, `제3자 서비스 미가입`), `들립니다` 비문 |
| P02 | 0.450 | 0.39 | 0.39 | **한 문장의 앞 절**(`복구하도록 노력하되,`) 통째 누락 + 질문 되풀이 |
| P08 | 0.452 | 0.80 | 0.27 | 조가 2문장(123자)인데 **앞 문장 통째 누락** |
| P09 | 0.468 | 0.37 | 0.65 | 안 물어본 `사용·저장·수정·복제·공중송신·전시·배포` 열거를 끌어옴 |
| P10 | 0.746 | 0.56 | 0.82 | 질문 되풀이·중복 서술 |

**리콜 손실 3건 + 정밀도 손실 2건.** 두 방향 모두 고쳐야 한다.

### 14-3. 천장 계산 — 어디까지 올릴 수 있나

정답 조의 원문을 절 단위로 골라 쓰는 전략의 상한을 greedy oracle로 쟀다(어절 F1 기준).

| | 평균 어절 F1 |
|---|---|
| 현행 답변 | 0.623 |
| 문장 단위 추출 | 0.747 |
| **절 단위 greedy oracle** | **0.815** |
| 손으로 쓴 목표 답변 10개 | 0.879 |

**"정답 조의 필요한 절을 하나도 빠짐없이, 원문 표현 그대로, 그 외에는 아무것도 쓰지 않는다"** — 이것이 목표 답변 형태다.

### 14-4. 왜 기존 커버리지 재시도(V7)가 이걸 못 잡았나

`_cov_find_missing`이 누락 3건을 전부 놓쳤는데, 이유가 셋 다 달랐다.

- **P02** — 커버리지 단위가 *문장*이었다. `…노력하되, 2시간 이상 …알려 드리겠습니다.`는 한 문장이라 뒤 절만 답해도 bigram 겹침이 `_COV_PRESENT=0.45`를 넘겨 **"이미 반영됨"으로 통과**했다. gold key_fact는 이 한 문장에서 2개가 나온다.
- **P08** — 조가 2문장뿐인데 앞 문장의 질문 관련도가 하위라 필수 후보에 들지 못했다. 짧은 조는 모든 문장이 정답 구성 요소다.
- **P07** — 같은 사실을 반복하는 다른 문장이 `0.75 × top` 아래였다.

세 실패 모두 "관련도가 낮아서"가 아니라 **필수 판정을 관련도 하나로만 했기 때문**이다.

### 14-5. 조치

| # | 변경 | 겨냥 |
|---|---|---|
| 1 | `_SYSTEM_PROMPT` 재작성 — 원문 통째 인용(1), 질문 되풀이 금지(2), `~하되,`로 이어지는 문장 전체 인용(3), 같은 규정의 다른 문장 동반(4), 안 물어본 열거 금지(5) | 전 문항 |
| 2 | `_split_coverage_units()` 신설 — 커버리지 단위를 문장 → **절**로. `~하되,/~하며,/~으며,` 뒤에서 추가 분할. 근거 머리글·목록 도입문(`…아래와 같습니다.`) 제외 | P02, P03 |
| 3 | `_cov_find_missing()` 재작성 — 필수 절을 **세 경로**로 수집: (1) 질문 관련도, (2) 짧은 근거(220자 이하)면 전량, (3) 이미 인용한 절의 **형제 절**. 리랭커 없어도 어휘 폴백으로 동작(기존엔 통째 비활성) | P02, P07, P08 |
| 4 | `_COV_PRESENT` 0.45 → **0.56** | P02 |
| 5 | `_is_short_evidence()` + 짧은 조 전문 인용 지시 (200자 이하 단일 조) | P05, P08 |
| 6 | `_trim_long_structural_block()` — 400자 넘는 항은 항 안에서 다시 문장 단위로 좁힘. 목록 질문은 제외 | P09 |
| 7 | 6번 셀 신설 — 어절/집합 F1 + P/R + 빠진 토큰·군더더기 토큰 출력 | 측정 |

관련도 문턱(`_COV_REL_RATIO=0.75`, `_COV_ABS_MIN=0.45`)은 리랭커 점수 스케일에 맞춰 튜닝된 값이라 **손대지 않았다.** 432조합 스윕 결과 채택값 주변이 넓은 평지다.

### 14-6. 오프라인 검증 (현행 답변 + 새 커버리지 지목분, 어절 F1 근사)

| 문항 | 현행 | 근사 | 새로 지목된 절 |
|---|---|---|---|
| P02 | 0.391 | **0.610** | `이러한 경우에도 회사가 상황을 파악하는 즉시 …복구하도록 노력하되,` |
| P07 | 0.280 | **0.472** | `통합서비스: … 회사가 아닌 카카오 계열사에서 제공하는 서비스 …포함되지 않습니다` |
| P08 | 0.400 | **0.935** | `본 약관에 규정되지 않은 사항에 대해서는 …규정에 따릅니다.` |
| P06 | 0.836 | 0.806 | (오탐 1건 — 의미상 이미 답한 문장) |
| 나머지 6문항 | — | 변화 없음 | 지목 없음 |
| **평균** | **0.623** | **0.715** | |

이 근사는 **비관적**이다. 지목된 절을 원문 그대로 뒤에 붙였을 때의 값이고, 실제로는 모델이 문장에 녹여 쓰므로 정밀도 손해가 더 작다. 6번 셀로 실측할 것.

### 14-7. 다음 확인 사항

- Colab T4에서 6번 셀 실측 — 평균 어절 F1이 0.71 이상인지, P06이 실제로 퇴행하는지.
- 재시도 1회가 늘어 문항당 지연이 커진다. p50 21s / 문항 타임아웃 120s이므로 여유는 있으나 `RETRY_DEADLINE_S=35.0`에 걸려 보완이 생략되는 문항이 없는지 로그 확인.
- P04(0.741)·P10(0.746)은 이번 조치로 안 움직인다. 남은 손실은 질문 되풀이라 프롬프트 규칙 2의 효과에 달렸다.
- judge의 completeness 감점 3건(P02·P07·P08)이 F1 누락 3건과 **정확히 같은 문항**이다. 커버리지가 고쳐지면 judge 97.45도 함께 오른다(최대 +2.55 × 0.5 = +1.3점).

---

## 15. 공식 채점 결과로 확정한 것 (2026-08-12)

공식 채점 결과 데이터(`practice_result_*.json`)를 열어 판정 심판의 축별 사유를 직접 읽고, §10-2의 인용 표기 판단을 뒤집었다. 아래는 전부 로컬에서 재현·검증한 값이다.

### 15-1. 본문 조항 표기는 넣는 게 맞다 — §10-2 뒤집음

§10-2는 "본문 인용은 F1 −0.135라 기각"이라고 결론냈는데, 그 −0.135는 **문서명+조번호를 문장으로 서술하는 무거운 형태**의 비용이었다. 결정을 뒤집은 근거는 **우리 팀 공식 채점 결과 데이터**다.

- 공식 판정 심판의 **근거성 축 사유가 본문 조항 표기를 핵심 신호로 명시**하고 있었다. 사유 문구가 "답변 서두에 '(제N조)'를 명시하여 정답 조에 정확하게 근거하고 있음을 밝히고 있습니다" 형태로, 근거성 만점의 직접 근거로 조항 표기를 든다.
- 우리 자체 규칙 기반 평가기는 근거성을 `retrieved` **순위로만** 계산해서 이 신호를 구조적으로 못 본다. §10-2 판단이 그 사각지대에서 나온 것.
- **비용 재측정(최소 형태 `(제N조)`):** 제출 답변 10개에서 인용 접두를 제거해 F1을 다시 재니 0.7514 → 0.7661, **비용 −0.0147(30점 환산 −0.44점)**. §10-2의 −0.135(무거운 형태)의 약 1/9.
- 그래서 **근거 명확성을 올리려고 `(제N조)` 최소 형태로 넣기로** 결정. 문서명까지 붙이면 비용이 약 2배(−0.90점)로 커지므로 조 번호만 짧게 쓴다.

정확한 표현: "F1만 보면 손해지만, 우리 채점 결과에서 근거성이 조항 표기 명확성으로 매겨지는 걸 확인해 F1 0.44점을 근거 명확성과 교환했다."

### 15-2. 자체 F1 근사의 캘리브레이션 — 두 번째 검증

§14-1에서 채점식을 역산(MSE 0.0015)했는데, 외부 답변 파일 하나로 재검증했다.

- 외부 고득점 답변 세트를 우리 평가기에 그대로 넣어 공식 per-question F1과 대조: **평균 절대오차 0.0228**, 평균 편차 **+0.0152**(우리가 살짝 낙관적).
- 귀결: 우리 자체 F1 **0.7514는 공식 기준 약 0.736**으로 봐야 한다. 발표·기록에 0.7514를 쓰되 "자체 근사, 공식 대비 +0.015 낙관" 각주를 단다.

### 15-3. 성능(속도)은 채점에서 비활성 — 공식 근거 확보

PDF p14의 "속도 배점 없음"보다 강한 직접 증거를 확인했다.

- 공식 결과 파일에 `"performance": {"status": "disabled", "score_0_100": null}`, `total_0_100`이 `quality_score_0_100`과 동일.
- 성능 구간은 답변 캐시로 p50 2.2ms까지 만들 수 있는 값이라(실측 사례 존재) 순위와 무관. 우리 p50 22초는 **매 요청 실제 추론**한 값이고, 배점이 없으니 이 여유를 품질로 환전한 판단이 맞다.

### 15-4. 남은 손실은 세 문항, 세 기전 — 고득점 답변과 대조

고득점 답변 세트(공식 F1 0.8617)와 우리 답변(자체 0.7514)을 문항별로 대조하니 격차가 **P02·P07·P09 세 문항**에 몰려 있고, 처방이 셋 다 다르다.

| 문항 | 우리 P/R | 기전 | 처방 방향 |
|---|---|---|---|
| P02 (+0.54) | 0.43/0.43 | 한 문장의 **앞절 통째 누락** + 질문 되풀이 | 더 담기(절 분할 게이트가 왜 안 걸렸는지 로그) |
| P07 (+0.32) | 0.78/**0.50** | 3문장 중 1문장만, 예시를 `(예:…)` 괄호로 압축 | 더 담기(예시를 독립 문장으로 펴기) |
| P09 (+0.29) | **0.40**/0.81 | 안 물어본 열거("사용·저장·수정…") 끌어옴 | 덜 담기(프롬프트 규칙 2 강화) |

- **P02 검색은 정상**: BM25 RRF 상위 3개가 gold 3개 전부. 순수 생성 문제로 확정.
- **`retrieved` 개수 재검토 여지**: 고득점 세트는 문항당 2~4개(평균 3.2)를 반환하고도 MRR 1.0. §9-8의 "회수 확대 이득 없음"은 *우리 생성기가 k=1에 맞춰졌을 때* 내린 결론이라, 다중 근거가 생성 리콜을 돕는지 재실험 가치가 있다.

### 15-5. 다음 확인 사항 (갱신)

- **P02·P07·P09 재시도 로그** — 커버리지가 위 세 절을 지목했는지 / 채택 게이트를 넘었는지 / 35초 마감에 걸렸는지 구분. 처방이 갈린다.
- **프롬프트 규칙 추가 A/B** — "여러 절로 이어지는 문장은 절마다 독립 문장으로, 예시·단서를 괄호로 압축하지 말 것"(P02·P07 겨냥). A/B는 하나씩.
- **`retrieved` k 확대 A/B**(`min_k=1→2`) — P02·P07 리콜이 오르는지.
- **평가기 Gemini 스모크 테스트** — 여전히 미실행. 공식 결과 JSON 구조(`judges.gemini.per_question[].axes.*.{score,reason}`)와 우리 평가기 출력 형태 대조.
